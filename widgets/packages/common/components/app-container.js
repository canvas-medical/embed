import styled from 'styled-components'

/*
 * Centralized app container. If we need to make any
 * adjustments to the global container, do them here
 */

export const AppContainer = styled.div`
  height: 100vh;
`
